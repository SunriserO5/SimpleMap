{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run (MSVC Debug)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/simplemap1_gui.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "Build (MSVC)"
        }
    ]
}
